@* Generator: MvcView GeneratePrettyNames: true *@
@using Advertise.Core.Extensions
@using Advertise.Service.Services.File
@using Auth = Advertise.Service.Services.Permissions.AccessPermission.AccessPermissionConst;
@model Advertise.Core.Models.Role.RoleViewModel
@{
    Layout = null;
}
<div class="landing-items role-items">
    <div class="product-item column landing-column">
        <div class="part-nine">
            @if (User.IsInRole(Auth.CanEditRole) || User.IsInRole(Auth.CanDeleteRole))
            {
                <div class="item-edit-wrapper">
                    <i class="fa fa-pencil-square"></i>
                    <ul class="item-edit-list">
                        @if (User.IsInRole(Auth.CanEditRole))
                        {
                            <li>
                                <a href="@Url.Action(MVC.Role.Edit(Model.Id))">
                                    <span>@Admin.Edit</span>
                                    <i class="fa fa-pencil fa-flip-horizontal"></i>
                                </a>
                            </li>
                        }
                        @if (User.IsInRole(Auth.CanDeleteRole))
                        {
                            <li>
                                <a href="#" data-action="removeItem" data-event="click" data-param="Role" data-param-id="">
                                    <span>@Admin.Delete</span>
                                    <i class="fa fa-trash"></i>
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            }
        </div>
        <div class="product-preview-actions">
            <figure class="product-preview-image">
                <div class="item-preview-image">
                    <img src="@FileConst.Role" alt="@Admin.UserRole.PlusWord(" ").PlusWord(Model.Name)">
                </div>
            </figure>
        </div>
        <a class="novinak-title-link" href="javascript:void(0)">
            <p class="novinak-title text-header">@Model.Name</p>
        </a>
        <p class="part-two"><i class="fa fa-calendar-check-o"></i> <span>@Admin.InsertDate.PlusWord(":")</span></p>
        <p class="part-three">@Model.CreatedOn</p>
        <div class="clearboth"></div>
    </div>
</div>